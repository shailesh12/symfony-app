{% extends 'base.html.twig' %}

{% block title %}Center{% endblock %}

{% block body %}
    <h1>Center</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ center.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ center.name }}</td>
            </tr>
            <tr>
                <th>Center_id</th>
                <td>{{ center.centerId }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ center.type }}</td>
            </tr>
            <tr>
                <th>Point_of_contact</th>
                <td>{{ center.pointOfContact }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ center.address }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('center_index') }}">back to list</a>

    <a href="{{ path('center_edit', {'id': center.id}) }}">edit</a>

    {{ include('center/_delete_form.html.twig') }}
{% endblock %}
